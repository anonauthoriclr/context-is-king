# Figure / analysis layer (CPU only).
# With the cached artifacts in data/ (see data/README.md), these packages are
# enough to regenerate every figure in the paper and inspect the compact table
# outputs. No GPU or model weights are required.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Optional extras:
#   plotly  -- interactive 3-D HTML views (shape_plot3d.py, build_plotly.py)
#   ripser  -- recomputing persistent-homology summaries from extracted arrays
# plotly>=5.0
# ripser>=0.6
